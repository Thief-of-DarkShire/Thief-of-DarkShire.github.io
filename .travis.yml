sudo: false
language: node_js
node_js:
  - 10 # use nodejs v10 LTS
cache: node_modules
branches:
  only:
    - dev # build dev branch only
script:
  - hexo generate # generate static files
after_script:
  - git config user.name "ch4o5"
  - git config user.email "li_dongyun@outlook.com"
  - hexo deploy